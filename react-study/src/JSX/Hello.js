import Names from "./names";
const fullnames = {
    chan: 'Chan',
    jong: 'Jong',
    oxix: 'JongChan'
};

const list = (
    <ul>
        {/*{Names(fullnames).map(props => <li>{props}</li>)}*/}
    </ul>
);


